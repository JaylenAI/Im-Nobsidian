# 개발 일지 — 2026-06-02

> `/goal` 동기화 충실도 미션. 롤백 대신 **전진 수정**을 확정(ADR-007)하고,
> 충실도 회복을 6-Phase 로드맵으로 구조화. 이날은 **Phase 1 — Frontmatter/Relation
> 충실도 봉합**을 구현·검증. 분기 `fix/pull-resolution-fidelity`.

## 0. 결정: 전진 수정 (ADR-007)

v0.1.12 롤백 vs HEAD 전진 수정을 git 고고학으로 비교. v0.1.12 는 데이터 손실
결함 4건(결함5/7/11/12) 미수정 + 관측 회귀 2건은 이미 HEAD에서 수정됨 + 롤백 시
66 커밋·31 테스트 폐기 → **전진 수정 채택**. 상세는 `adr/007-fix-forward-vs-rollback.md`.

## 1. Phase 1 범위 — relation 충실도 5결함(M1~M5)과 cover-URL

Notion→Obsidian pull 후 frontmatter/본문의 링크가 실제 파일을 못 가리키거나
UUID 그대로 남는 결함군. 이번 Phase는 **M2·M3·M4 + cover-URL + relation 후처리**를 봉합.

### M4 — 단일 패스와 후처리 패스의 위키링크 텍스트 불일치 ⭐

- **증상**: 같은 페이지가 변환 단일 패스(`resolvePageId`)에서는 StateDB 원시 제목
  (`.title`)으로, 후처리 패스(`resolveNotionLinks`)에서는 파일 basename 으로 해소돼
  서로 다른 `[[..]]` 텍스트가 생성. 원시 제목에 슬래시·콜론 등 파일명 금지문자가
  있으면 단일 패스 링크가 실제 파일을 못 가리키는 깨진/이중 위키링크가 됐다.
- **수정**: SSOT 유틸 `utils/wikilink-title.ts` 신설 —
  `wikilinkTitleFromPath(obsidianPath)` 가 `.md` 제거 + basename 추출을 단일 규칙으로
  강제. `orchestrator`·`database-syncer` 의 `resolvePageId` 람다와
  `resolveNotionLinks` 의 `idToTitle` 빌드가 **모두 이 함수를 통과**.

### M3 — 후처리 대상 경로를 슬라이스로 추정 → 실측 ⭐

- **증상**: pull 후 링크 후처리 대상(`writtenPaths`)을 "synced 상태 db-row 중
  마지막 N개" 슬라이스로 추정. 정렬·기존행 혼입 탓에 엉뚱한 파일을 후처리하거나
  갓 쓴 행을 놓쳐 링크가 미해소로 남았다.
- **수정**: `DatabaseSyncResult` 에 `writtenPaths: string[]` 추가. `pullDatabase`
  루프가 실제 기록된 행 경로(`pullDatabasePage` 의 `{action:"written", path}`)를
  수집해 반환. orchestrator 의 두 수집 지점(설정 DB `pullAll`, 디스커버리
  `pullDiscoveredDatabases`)이 슬라이스 대신 `dbResult.writtenPaths` 를 그대로 사용.

### M2 — 디스커버리 전용 조기 반환이 링크 후처리를 건너뜀 ⭐

- **증상**: 본문 페이지 변경 0 + 설정 DB 0 인 핫패스(Test 볼트)에서, 디스커버리된
  DB 행을 기록(`writtenPaths` 채움)하고도 `resolveNotionLinks` 없이 조기 반환 →
  그 행들의 본문 링크·frontmatter relation 이 UUID 그대로 잔존.
- **수정**: pull()의 모든 종료 경로를 단일 `finalize(created, updated, deleted)`
  헬퍼로 모음 — `resolveNotionLinks(writtenPaths)` + 메타 3종 마킹 + `PullResult`
  조립을 한 곳에서. 디스커버리 조기 반환과 메인 반환이 모두 이 헬퍼를 경유.
  (부수효과: 조기 반환의 imageCount/fileCount 하드코딩 0 도 실측값으로 교정)

### cover-URL — 원격 URL 을 위키링크로 잘못 래핑

- **증상**: DB 행 cover 처리에서 이미지 다운로드가 비활성/실패해 원격 `https://` URL이
  그대로 돌아올 때도 `[[https://..]]` 로 감싸 존재하지 않는 파일을 가리키는 깨진 링크.
- **수정**: 다운로드가 **로컬 첨부 경로로 치환했을 때만** `[[..]]` 로 감싸도록 가드
  추가(`!/^https?:\/\//i.test(resolved)`). 원격 URL 이면 평문 URL 로 보존.

### relation 후처리 (이전 세션 산출물, 본 커밋에 동봉)

- `sync/frontmatter-link-resolver.ts` — frontmatter 의 relation/people 원시 UUID를
  `[[제목]]` 으로 해소하는 순수 헬퍼(gray-matter 캐시 회피 위해 fresh 객체 생성).
  `resolveNotionLinks` 후처리에 배선됨. 변환 시점엔 대상 페이지가 미등록이라 UUID로
  남지만, 이 post-pass 시점엔 id→title 맵이 완성돼 해소.

## 2. 코드 변경 요약

| 파일                                | 변경                                                                                       |
| ----------------------------------- | ------------------------------------------------------------------------------------------ |
| `utils/wikilink-title.ts`           | **신규** — M4 SSOT (15줄)                                                                  |
| `sync/frontmatter-link-resolver.ts` | **신규** — relation 후처리 순수 헬퍼 (67줄)                                                |
| `sync/database-syncer.ts`           | `writtenPaths` 인터페이스/수집/반환 6곳, M4 resolver 통일, cover-URL 가드 (+47/-)          |
| `sync/orchestrator.ts`              | `finalize()` 헬퍼, M2 분기·메인 반환 라우팅, M3 수집 2곳, M4 idToTitle/resolver (+118/-69) |

## 3. 회귀 테스트 (영구 잠금)

기존 821 테스트가 수정 전/후 모두 통과 = 이 결함 경로들이 **미커버**였다는 증거.
결함별 회귀 테스트 **15건** 신규:

- `tests/utils/wikilink-title.test.ts` — M4 SSOT 7건 (basename/확장자/금지문자 불변식)
- `tests/sync/database-syncer.test.ts` — writtenPaths(M3) 4건 + cover-URL 가드 2건
- `tests/sync/orchestrator.test.ts` — M2 finalize 라우팅 2건
  (디스커버리 기록 시 후처리 호출 / 기록 0건 시 미호출)

## 4. 결과물 (정량)

- **테스트: 836 passed / 0 failed** (821 → 836, +15)
- typecheck(`tsc --noEmit`) 클린, lint(src+tests) 위반 0, build(tsup) 성공
- 변경: 4 파일 수정(+274/-69) + 4 파일 신규(217줄)

## 5. 미해결 / 다음 작업

- **라이브 검증(Phase 5)**: 실데이터 fresh E2E pull 로 깨진 링크 수(목표 21→0)·
  relation UUID 해소·repull churn 0 을 실측. 측정 스크립트는 볼트 전체 파일을
  링크 대상에 포함하고 http-래핑 vs 미존재-대상을 구분하도록 정밀화 필요.
- **잠재 이슈(범위 외, 문서화)**: `filtered.length===0` 인데 **설정 DB 가 존재**하는
  분기는 현재 `emptyResult` 로 조기 반환 → 본문 변경이 없으면 설정 DB pull 을
  통째로 건너뛴다. 본 Phase 범위 밖이며 별도 추적.
- M1·M5 는 본 Phase 미포함 — P0 나머지 봉합(②)에서 처리 예정.

## 6. 100% 로드맵 (재확인)

Phase 1(완료·커밋) → **2(M1 페이지모드 resolver + M5 대괄호 — 본 일지 §7)** →
3(#74 본문 컨테이너, UX/시각 트랙) → 4(#75 DB↔Bases 인라인) →
5(#77 측정 인프라 + 라이브 전수 검증) → 6(#78 dev→main + v0.2.0 태그).

---

## 7. Phase 2 — P0 relation 잔여 봉합 (M1 페이지모드 resolver + M5 대괄호)

> 분기 `fix/p0-relation-residual`. Phase 1 의 잔여 P0(relation churn) 봉합 + 방어적 대괄호 봉합.

### M1 — 페이지 모드 pull 의 relation/people 이 매 pull 마다 raw UUID 로 재생성 ⭐

- **증상**: 페이지 모드(비 DB) pull 에서 relation/people 속성이 `[[제목]]` 이 아니라
  raw Notion UUID 로 프론트매터에 기록 → 후처리(`resolveNotionLinks`)가 매번 다시
  `[[제목]]` 으로 고쳐 쓰는 **2-write churn**. 즉 멱등이 깨진다.
- **원인**: 페이지 모드는 `notionClient.extractProperties()` → `client.propertyMapper`
  를 쓰는데, 이 mapper 에는 `WikilinkResolver` 가 **한 번도 주입된 적이 없다**. DB 모드는
  `orchestrator.propertyMapper`(resolver 주입됨)·DatabaseSyncer(자체 주입 mapper)를 써서
  이미 면역이었다 — 그래서 그동안 페이지 모드에서만 잔존.
- **해결(Design B, SSOT)**: orchestrator 생성자에서 **단일** resolver 객체를 만들어
  `this.propertyMapper` 와 `this.notionClient` 양쪽에 주입. client 에는 위임 메서드
  `setWikilinkResolver()` 추가(중복 헬퍼 없이 mapper 로 위임). resolver 의 `resolvePageId`
  는 raw 제목이 아니라 `wikilinkTitleFromPath(obsidianPath)` 로 해소해 M4 SSOT(파일명
  basename = 위키링크 텍스트)와 정합 유지.

### M5 — 제목 대괄호가 `[[..]]` 위키링크를 조기 종료 (방어적 봉합)

- **증상**: 제목에 `[`/`]` 가 있으면(예 "Attention [2017]") 생성 위키링크가
  `[[Attention [2017]]]` 이 되고, Obsidian 파서가 **첫 `]]`** 에서 끊어 링크 대상을
  "Attention [2017" 로 오인 → 깨진 링크. 빈도는 낮지만 객관적 문법 충돌.
- **원인**: `sanitizeFileName` 의 `INVALID_CHARS` 에 대괄호가 없어 파일명·basename 에
  대괄호가 보존됨. basename 에서 파생되는 위키링크 텍스트에도 그대로 새어 들어감.
- **해결**: 파일명 SSOT 인 `sanitizeFileName` 한 곳에서 대괄호를 `_` 로 치환
  (`WIKILINK_UNSAFE_CHARS` 상수 분리 — 파일시스템 무효 문자와 위키링크 무효 문자는
  관심사가 다름). `(`/`)` 매핑은 기존 "(2017)" 파일과 충돌 위험이 있어 배제, `_` 는
  충돌·멱등 모두 안전. 원본 제목은 frontmatter `title` 에 보존되므로 **무손실**.
  별칭 구분자 `|` 는 이미 `INVALID_CHARS` 가 처리함을 확인.

### 코드 변경 요약 (Phase 2)

- `notion/client.ts`: `setWikilinkResolver()` 위임 메서드 추가 + `WikilinkResolver` import
- `sync/orchestrator.ts`: 생성자에서 단일 resolver 를 mapper·client 양쪽 주입(M1)
- `utils/sanitize.ts`: `WIKILINK_UNSAFE_CHARS = /[[\]]/g` 추가, 대괄호 → `_` 치환(M5)
- 테스트 목 갱신: `tests/helpers/mock-orchestrator.ts`·`tests/sync/compute-patches.test.ts`
  에 `setWikilinkResolver: vi.fn()` 추가(시그니처 드리프트 방지)

### 회귀 테스트 (영구 잠금, +7)

- **M1 (4건, `tests/sync/orchestrator.test.ts`)**: 생성 시 client 에 resolver 1회 주입 /
  `resolvePageId` 가 raw 제목 아닌 basename 으로 해소(M4 SSOT) / 미매핑 id 는 null →
  후처리 안전망 위임 / `resolve` 가 제목→notionPageId 해소.
- **M5 (3건)**: 대괄호 `_` 치환 + 멱등(`tests/utils/sanitize.test.ts` 2건), 대괄호
  제목이 조기 종료 없는 위키링크 본문으로 수렴하는 불변식(`tests/utils/wikilink-title.test.ts` 1건).

### 결과물 (정량)

- **테스트: 843 passed / 0 failed / 11 skipped** (836 → 840 [M1] → 843 [M5], +7)
- typecheck(`tsc --noEmit`) 클린, eslint(변경 src) 위반 0
- 변경: 8 파일(src 3 + 테스트 5), 신규 파일 0

### 다음 작업

- 커밋(M1 / M5 / docs 분리) 후 dev 머지·push·태그는 **승인 게이트**.
- Phase 3(#74)는 데이터 무손실이 확인된 UX/시각 트랙 — 충실도 미션 범위 포함 여부
  사용자 확인 필요.

---

## 8. M6 + Phase 4 — 라벨형 mention 봉합 + 충실도 측정 인프라

> 분기 `fix/mention-page-labeled`(M6, 커밋 cf08b19) · `feature/fidelity-metrics`(#77, 커밋 12ac7b0).
> P0 잔여의 마지막 한 결함을 닫고, 충실도를 상시 회귀 잠금할 측정 인프라를 정식화.

### M6 — 라벨 동반형 `<mention-page url>제목</mention-page>` 미변환 잔류 ⭐

- **증상**: breadcrumb 의 페이지 멘션 중 URL+라벨 동반형
  `<mention-page url="...notion.so/<id>">제목</mention-page>` 이 변환되지 못하고
  원시 태그로 본문에 잔류. 실코퍼스 2건(`AI Engineer (1)/Study.md`→[[AI Engineer (1)]],
  `Empowerment (1)/Format/Format.md`→[[노션 단축키 모음]]).
- **원인**: `convertPageMentions` 가 ① `id="X">text</mention-page>`(→`[[text]]`)와
  ② self-closing `url="..32hex.."/>`(→`[[notion:id]]`)만 처리하고, ③ **라벨 동반 url형**
  분기가 없어 무시. (① id형 + 라벨, ② url형 + self-closing 만 커버하던 비대칭)
- **해결**: ②·③ 을 **하나의 통합 정규식**으로 흡수 — lazy `[^>]*?`(>를 넘지 않음)
  - 분기 `(?:/>|>[\s\S]*?</mention-page>)`. 둘 다 `[[notion:id]]` 로 환원하고 이후
    `resolveNotionLinks` 가 id→제목 맵으로 정식 `[[제목]]` 해소(id 해소가 SSOT, 라벨 텍스트는 무시).
- **회귀 테스트 3건**(`tests/converter/enhanced-md-converter.test.ts`): self-closing /
  라벨 동반형 / 한 줄 다중(lazy 과잉소비 방지) 모두 개별 변환되는 불변식.

### Phase 4 (#77) — 충실도 측정 인프라 (회귀 상시 잠금)

릴리스 전 산발적으로 짠 `/tmp/*.mjs` 측정 스크립트를 **엔터프라이즈 모듈로 정식화**.

- `src/audit/fidelity.ts` — 순수 함수 `classifyBodyFidelity(body, ctx)`: 본문의 notion
  링크/멘션을 ① **보존마커**(`[..](url)%%im-nobsidian:..%%`, 의도된 무손실) ② **외부**
  (코퍼스 밖, 정당) ③ **자기참조**(블록 앵커 등, 정당) ④ **결함**(동기화된 _다른_
  페이지로의 미해소 링크/멘션)으로 분류. `summarizeFidelity` 로 코퍼스 합산. 마커/ID
  판별은 SSOT 재사용(`MARKER_BRAND`·`compactNotionId`) — 중복 헬퍼 없음.
- `scripts/audit-vault.mjs` — 실 vault 에 대고 충실도(결함=0) + **멱등성**(disk sha256
  == `sync_state.content_hash`) + 디스크 누락을 한 번에 측정, 위반 시 종료코드 1 → CI 게이트.
- **회귀 테스트 12건**(`tests/audit/fidelity.test.ts`): 보존마커(압축/공백형)/외부/자기참조/
  cross-page md-link 결함/라벨형 mention 결함(M6 가드)/self-closing/정상 0결함/다중 lazy/
  titleById 부재/summarize 합산.

### 디스커버리 이중경로 무손실 검증 (부수 성과 — 버그 아님)

- **경위**: M6 적용 fresh pull 진행률이 `[N/241]` 로 표시돼 직전 결과(1159 파일) 대비
  79% 손실로 오인 → 진행 중 pull 중단. 코드 포렌식으로 **오독** 규명.
- **규명**: pull `total` 은 *발견 페이지 수*이고 DB 행은 side-effect.
  `getChildPages` 가 `child_page` 만 필터(child_database 제외)하므로 **subtree 순회**
  (`getChildPagesRecursive`)·**search 폴백**(`getPagesUnderRootViaSearch`, `object:page`만)
  둘 다 발견=241(file 172 + folder-note 69). DB 행 918 은 양 경로 공통으로
  child_database 자동발견 → DatabaseSyncer 확장으로 생성.
- **결론**: 두 디스커버리 경로는 동일 무손실(코드 주석 "무손실" 주장 옳음). 디스커버리
  수정 **불필요**. 교훈: 진행률 카운터(발견 페이지)를 전체 파일로 해석 말 것 — 측정은
  카운터가 아니라 `audit-vault.mjs`(실파일 + sync_state 대조)로.

### 결과물 (정량)

- **테스트: 858 passed / 0 failed / 11 skipped** (846 → 858, +12 audit)
- 백업(pre-M6) 실측: 1159 파일 · 보존마커 414 · 외부 36 · 자기참조 1 · **결함 2** · churn-0(1159/1159)
  · 디스크 누락 0. 임시본의 "결함 3" 은 실은 cross 2 + self-breadcrumb 1(러너가 path→id 정밀 분리).
- 디스커버리 구성 실측: db-row 918 + file 172 + folder-note 69 = 1159.

### 다음 작업

- **라이브 전수 재검증(진행 중)**: M6 적용 fresh pull 완주 → `audit-vault.mjs` 로 결함 2→0
  확인 + 2차 pull 로 재변환 멱등성 측정. QA 리포트는 실측 후 작성.
- 검증 통과 시 fix·feature 브랜치 → dev 통합, **dev→main + v0.2.0 태그는 승인 게이트**.
